<% if record.errors.any? %>
  <div class="error-notifications">
    <h2><%= pluralize(record.errors.count, "error") %> prohibited this user from being saved:</h2>

    <ul>
    <% record.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>
