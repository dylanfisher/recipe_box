<%= simple_form_for([:admin, @color_scheme]) do |f| %>
  <%= render 'admin/errors', f: f, record: @color_scheme %>

  <div class="form-inputs">
    <%= f.input :title %>
    <%= f.input :color %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
